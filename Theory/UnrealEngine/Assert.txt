

check(표현식);

checkf(A != nullptr, TEXT("%s가 NULLPTR이었습니다.", A.name());


() 안에 들어가는 식이 거짓(0, nullptr 등)인 경우, 경고를 띄움. 게임을 중단하지는 않음.
디버그 모드에서만 사용 가능.
디버깅을 위해 사용하는 기능.

checkf(표현식, 메세지); 추가 메세지를 로그에 출력

ensure(표현식);
() 안에 들어가는 식이 거짓(0, nullptr 등)인 경우, 게임을 중단함.
ensureMsgf(bool, Msg) 형태를 사용하면, 두 번째 인자에 입력된 문자열을 로그에 띄워줌.
디버그, 릴리즈 모두 사용 가능하며, 디버그보다는 프로그램의 안정성 유지를 위해 사용됨.

ensure(표현식, 메세지); 추가 메세지를 로그에 출력

if(ptr == nullptr)
{
msgassert(~~)
}