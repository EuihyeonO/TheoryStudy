

컴파일 과정에서 함수들을 구분짓기 위해 컴파일러가 함수명을 개조하는 것.
오버로딩되어 이름이 동일한 함수를 구분짓기 위해 사용된다.
작명 규칙은 컴파일러마다 다르게 설정되어 있으며 호출 규약, 파라미터, 함수명 등을 기반으로 작명하게 된다.

C++은 오버로딩이 존재하기에 네임 맹글링이 필수적이지만,
C의 경우엔 그렇지 않다.

C와 C++을 혼용하게 될 경우, C스타일의 함수에는 extern "C" 키워드를 사용하여
불필요하게 네임 맹글링을 적용하지 않도록 하자.